package signin

import (
	"testing"
)

func Test_To(t *testing.T) {
	/*a := component.SignInClient{}
	p := unsafe.Pointer(&a)
	var b reflect.Type
	b = reflect.TypeOf(a)

	v := reflect.NewAt(b, p)

	for i := 0; i < v.NumMethod(); i++ {
		fmt.Println(v.Method(i).Type())
	}*/

	//fmt.Println(v.MethodByName("RegisterMethod").String())
	//pc := v.Interface().(*module.InNetClient)
	//reflect.Complex64
	//c := reflect.Value{}
	//c.SetPointer(p)
	//fmt.Println(c)
	//fmt.Println("end,", pc)

	/*a := pactum.GatewayRegisterRequest{}
	msgType := proto.MessageType("pactum.GatewayRegisterRequest")

	protokey := reflect.Indirect(reflect.New(msgType.Elem())).Addr().Interface().(proto.Message)

	fmt.Println("-----------------------------------------", a)
	fmt.Println(msgType)
	fmt.Println(reflect.TypeOf(protokey))*/
}
